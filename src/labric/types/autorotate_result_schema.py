# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class AutorotateResultSchema(UniversalBaseModel):
    """
    The estimated display rotation aligning the image with the reference.

    `applied` is False when the estimate's confidence fell below the
    persistence floor: nothing was stored and the caller should treat the
    image as not autorotated.
    """

    degrees: float
    snapped: bool
    confidence: float
    applied: bool

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
